#include <stdio.h>

int main() {
    int n, i, j, x = 1;
    scanf("%d", &n);

    int total = n * (n + 1) / 2;
    x = total;

    for(i = 1; i <= n; i++) {
        for(j = 1; j <= i; j++) {
            printf("%d ", x--);
        }
        printf("\n");
    }

    return 0;
}
